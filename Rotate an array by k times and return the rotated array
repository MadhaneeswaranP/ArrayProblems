let arr=[1,2,3,8,9,10];
let k=3;
var arrayRotation=function (arr,num){
   let i=0;
    while(k>0){
    let len=arr.length;
    let temp=arr[len-1];
      for(let i=len-1;i>0;i--){
         arr[i]=arr[i-1];
      }
    arr[i]=temp;
    k--;
    }
    console.log(arr);
    
}
arrayRotation(arr,k);
